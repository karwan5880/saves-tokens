# Speculative decoding and provider batch modes

## Method

When a provider supports it, speculative decoding and asynchronous/batch APIs can reduce latency or price for suitable workloads.

## Limitation

These are infrastructure optimizations, not a substitute for sending less relevant context. Availability, pricing, and semantics vary by provider and change frequently; consult current official documentation.
